window.trainingSnippets = {
  "Code/C++14-17/C++17TemplateFeatures/20_FoldExpressions.md": "# Fold Expressions\n\n# Before C++17\nBefore C++17, in order to have a variable number of parameters for functions we needed to do something like this, with recursion.\n\n```C++\ntemplate<typename T>\nT adder(T v) {\n  return v;\n}\n\ntemplate<typename T, typename... Args>\nT adder(T first, Args... args) {\n  return first + adder(args...);\n}\n```\n\nWith the following definitions :\n\n## Parameter pack\ntypename... Args is called a template parameter pack, and Args... args is called a function parameter pack (Args is, of course, a completely arbitrary name and could be anything else).\n\n## Parameter pack expansion \nWe used adder with args..., it's called parameter expansion\n\n# In C++17\nIn C++17, it becomes more simple to do the same thing.\n\n```C++\ntemplate<typename... T>\nauto adder (T... args) {\n    return (... + args); // ((arg1 + arg2) + arg3) ...\n}\n```\n\nWe don't need recursion anymore. \nThe parenthesis around the return expression are compulsory.\n\n## Unary left fold and unary right fold\n### unary left fold\n(... + args) is an unary left fold, like in the exemple above.\n\nIt results in :\n((arg1 + arg2) + arg3) ...\n\nIt generalizes with any operator op (all operators except ., -> and [])\n(... op args)\nwhich expands to: ((arg1 op arg2) op arg3) op ...\n\n### unary right fold\n(args + ...)\n\nit results in \n(arg1 + (arg2 + arg3)) ...\n\nGeneralization\n(args op ...)\nwhich expands to: arg1 op (arg2 op . . . (argN-1 op argN))\n\n### Exemple of difference between left and right\nIf we use the unary left fold :\n\n``` C++\ntemplate<typename... T>\nauto adder (T... args) {\n    return (... + args); \n}\n```\nWe can do : \n```C++\nadder(std::string(\"hello\"), \"world\", \"!\");\n```\n\nIt evaluates ((std::string(\"hello) +\"world\") + \"!\")\n\nThe operator + is defined for standard strings as long as one of the operands is a std::string, and it returns a std::string. So in that case we have two times the addition between a std::string and a string literal, so it works.\n\nBut if we use the unary right fold\n``` C++\ntemplate<typename... T>\nauto adder (T... args) {\n    return (args + ...); \n}\n```\nIt evaluates (std::string(\"hello) + (\"hello\" + \"!\")).\nAdding two string literals is not allowed, so it is not compiling.\n\n## Note \nBecause in almost all cases evaluation from left to right is the intention, usually, the left fold syntax\nwith the parameter pack at the end should be preferred (unless this doesn’t work).\n\n## With initial value\nYou can specify an initial value for the unary left fold and the unary right fold.\nThey are then called binary left fold and binary right fold.\n\n### Binary left fold\n( value op ... op args )\nwhich expands to: (((value op arg1) op arg2) op arg3) op ...\n\n```C++\ntemplate<typename... T>\nauto adder (T... args){\n    return (0 + ... + args); \n}\n\nint main(){\n    adder(); //OK\n    adder(1,2,3); //OK\n}\n```\n\n### Binary Right fold\n( args op ... op value )\nwhich expands to: arg1 op (arg2 op ... (argN op value)))\n\n```C++\ntemplate<typename... T>\nauto adder (T... args){\n    return (args + ... + 0); \n}\n\nint main(){\n    adder(); //OK\n    adder(1,2,3); //OK\n}\n```\n\nThe operator op has to be the same on both sides of the ellipsis.\n\nThe binary left fold should also be the usual choice unless there is a reason to use the right fold.\n\n### Empty parameter pack\nIf there is an initial value, it is possible to have an empty parameter pack.\n\nIf there is no initial value : \n- If operator && is used, the value is true.\n- If operator || is used, the value is false.\n- If the comma operator is used, the value is void().\n- For all other operators the call is ill-formed\n\n### Example with a special initial value\n```C++\ntemplate<typename... T>\nvoid print (const T&... args)\n{\n    (std::cout << ... << args) << '\\n';\n}\n```\n\nSo here the initial value is std::cout.\n\nIn this example the strings are concatenated without spaces.\n\nIt's possible to add a helper function, that we apply to the arguments to add the space : \n\n```C++\ntemplate<typename T>\nconst T& spaceBefore(const T& arg) {\n    std::cout << ' ';\n    return arg;\n}\ntemplate <typename First, typename... Args>\nvoid print (const First& firstarg, const Args&... args) {\n    std::cout << firstarg;\n    (std::cout << ... << spaceBefore(args)) << '\\n';\n}\n\n```\n\nHere, (std::cout << ... << spaceBefore(args)) is a fold expression that expands to:\nstd::cout << spaceBefore(arg1) << spaceBefore(arg2) << ...\n\nWe can also use a lambda to define spaceBefore() inside print():\n\n```C++\ntemplate<typename First, typename... Args>\nvoid print (const First& firstarg, const Args&... args) {\n    std::cout << firstarg;\n\n    //lambda\n    auto spaceBefore = [](const auto& arg) {\n        std::cout << ' ';\n        return arg;\n    };\n\n    (std::cout << ... << spaceBefore(args)) << '\\n';\n}\n```\n\n## Fold expressions for types\n\nWe can also use fold expressions to deal with template parameter packs (an\narbitrary number of types passed as template parameters)\n\n```C++\n#include <iostream>\n#include <type_traits>\n\n// check whether passed types are homogeneous:\ntemplate<typename T1, typename... TN>\nstruct IsHomogeneous {\n    static constexpr bool value = (std::is_same<T1,TN>::value && ...);\n};\n\n// Or same thing but as a function template\ntemplate<typename T1, typename... TN>\nconstexpr bool isHomogeneous(T1, TN...)\n{\n    return (std::is_same<T1,TN>::value && ...);\n}\n\nint main(){\n\n    std::cout<<IsHomogeneous<int, double, decltype(42)>::value<<std::endl;\n    // In this case the fold expression that initializes the member value expands to:\n    // std::is_same<int,double>::value && std::is_same<int,decltype(42)>::value\n\n    std::cout<<isHomogeneous(43, -1, \"hello\", nullptr)<<std::endl; //with Class Template Argument Detection\n    // In this case the fold expression that initializes the member value expands to:\n    // std::is_same<int,int>::value && std::is_same<int,const char*>::value\n    // && std::is_same<int,std::nullptr_t>::value\n}\n```\n",
  "Code/C++Advanced/12_VariadicTemplates/variadicTemplates.md": "# Variadic templates\n\nOne of the new features of C++11 is variadic templates. Finally, there's a way to write functions that take an arbitrary number of arguments in a type-safe way and have all the argument handling logic resolved at compile-time, rather than run-time. \n\n## Basic Example\nA function that adds all of its arguments together :\n\n```C++\ntemplate<typename T>\nT adder(T v) {\n  return v;\n}\n\ntemplate<typename T, typename... Args>\nT adder(T first, Args... args) {\n  return first + adder(args...);\n}\n\nint main() {\n\n    long sum = adder(1, 2, 3, 8, 12, 14);\n\n    std::string s1 = \"x\", s2 = \"aa\", s3 = \"bb\", s4 = \"yy\";\n    std::string ssum = adder(s1, s2, s3, s4);\n\n    std::cout<<sum<<std::endl;\n    std::cout<<ssum<<std::endl;\n\n}\n```\n\nadder will accept any number of arguments, and will compile properly as long as it can apply the + operator to them.\n\n## Function in a recursive way\nWhen we call adder(1,2,3,8,12,14), the following succession of calls happen : \nT adder(T, Args...) [T = int, Args = <int, int, int, int>]\nT adder(T, Args...) [T = int, Args = <int, int, int>]\nT adder(T, Args...) [T = int, Args = <int, int>]\nT adder(T, Args...) [T = int, Args = <int>]\nT adder(T) [T = int]\n\nso :\n1 + adder(2,3,8,12,14);\n2 + adder(3,8,12,14);\n3 + adder(8,12,14);\n8 + adder(12,14);\n12 + adder(14);\n14\n\n## Parameter pack\ntypename... Args is called a template parameter pack, and Args... args is called a function parameter pack (Args is, of course, a completely arbitrary name and could be anything else).\n\n## Parameter pack expansion \nWe used adder with args..., it's called parameter expansion\n\n## With classes\n\n### Type parameter pack\n\n```C++\ntemplate<typename... T> class X{};\n\nint main() {\n    X<> a;                    // the parameter list is empty \n    X<int> b;                 // the parameter list has one item\n    X<int, char, float> c;    // the parameter list has three items \n}\n\n```\n\n### Non type parameter pack\n```C++\ntemplate<bool...T> class X{};\n\nint main() {\n    X<> a;                    // the parameter list is empty \n    X<true> b;                // the parameter list has one item\n    X<true, false, true> c;   // the parameter list has three items \n}\n\n```\n\n### Reminder, non type parameter\nExample with std::array\n\n```C++\narray<int, 5> ar1{{3, 4, 5, 1, 2}};\n```\n\nint is a type parameter, that describes the type of the values in thge array and 5, the size of the array, is a non type parameter (a value of type int).\n\n## sizeof... operator\n\nThe sizeof... operator is a special form of pack expansion. It simply returns the number of pack elements\n\n```C++\n#include <iostream>      \n\ntemplate <class... Ts>\nvoid printCount(Ts... args) {\n  std::cout << sizeof...(args) << std::endl\n}\n\n\nint main() {\n    \n    printCount(22, 12.4, \"!\");\n}\n```\n\n## links\nhttps://www.ibm.com/docs/en/zos/2.1.0?topic=only-variadic-templates-c11\nhttps://eli.thegreenplace.net/2014/variadic-templates-in-c/",
  "Code/C++Debutant/21_OOP_8_Virtual_Polymorphism/main.cpp": "//\n//  main.cpp\n//  lesson21\n//\n//  Created by Mael Derio on 13/01/2023.\n//  Copyright © 2023 Mael Derio. All rights reserved.\n//\n\n#include <iostream>\n#include \"Vehicle.h\"\n\nusing namespace std;\n\n//2.\nvoid show(Vehicle v)  //Présente le véhicule passé en argument\n{\n    v.display();\n}\n\n//4.\nvoid showDynamic(Vehicle const& v) //We don't modify the object so i added const\n{\n    v.display();\n}\n\nint main() {\n    \n    // Polymorphism : it means many forms. An object can have a different form depending on the context\n    // Overloading is already a form of (static) polymorphism. Also called static binding.\n    \n    //1.\n    Vehicle v(1000);\n    v.display();\n    \n    Moto m(1000, 300);\n    m.display();\n    \n    //2. Make a show function above the main that calls the display method\n    show(v);\n    show(m);\n    \n    // In that case, both objects will be seen as Vehicles\n    // Because the compiler cannot know when it compiles the function display above if the vehicle will be a vehicle or a child class like moto\n    // So it will call the Vehicle version of display\n    // Each function call can be evaluated, resolved in a static way, meaning at compilation time, or in a dynamic way, meaning at runtime\n    //In that case the function call to show is made at compile time\n    \n    //If we want the function call to be resolved at runtime, we need to use virtual methods\n    \n    //3. Add virtual to our display methods in the class AND change the type in show to references\n    // We don't have to add virtual in the child classes, as they inherit the virtual, but it's more clear in the code\n    // It's not necessary that all methods are virtual, just some of them can be virtual\n    // You shouldn't put virtual in the .cpp\n    \n    //4. Rewrite show with references\n    showDynamic(v);\n    showDynamic(m);\n    \n    //5. Another way with pointers\n    Vehicle *v1(nullptr), *v2(nullptr);\n    v1 = new Vehicle(1000);\n    v2 = new Moto(1000, 300);\n    \n    v1 -> display(); //vehicle\n    v2 -> display(); //moto\n    \n    // This code without virtual in front of display would show vehicle and vehicle\n    // Show that, removing the virtual keyword\n    \n    //We say that the method display has a polymorphic behavior\n    \n    //5. Which methods cannot be inherited :\n    // constructor and destructor\n    // all other methods can be inherited and have a polymorphic behavior\n    \n    // A constructor cannot be virtual, and cannot call virtual methods\n    // Make sense that when we are in the constructor, we know which class, which type we are building, so we don't need to choose at runtime\n    \n    //6.\n    // Add destructors\n    \n    Vehicle *myVehicle(nullptr);\n    myVehicle = new Car(1000, 4);\n    // We create a car and we put its adress in a vehicle pointer\n    // Possible because Car IS A Vehicle\n    // The opposite is not true\n    \n    myVehicle->display();  //Car\n    \n    delete myVehicle;      //We destroy the Car\n    \n    // Problem : it's the destructor of Vehicle that is called, and not the one from Car.\n    // In that case, it doesn't make a difference, it will work, but if there is some specifics to a car that needs to be destructed, it will create problems\n    \n    // If we want to call the destructor from car, we need to make the destructor virtual\n    // If we use polymorphism, the destructor of the class always needs to be virtual\n    \n    \n    return 0;\n}\n",
  "Code/C++Research/C++20/concepts.md": "Great question 👍 — **concepts** in C++20 are one of the most powerful new features. They let you **express constraints** on template parameters in a clean, readable way. Let’s break it down step by step.\n\n---\n\n# **1️⃣ Problem Before C++20**\n\nTemplates are very flexible, but errors can be **cryptic**.\n\nExample (pre-C++20):\n\n```cpp\ntemplate<typename T>\nT add(T a, T b) {\n    return a + b;\n}\n\nint main() {\n    std::cout << add(1, 2) << \"\\n\";       // OK\n    std::cout << add(\"hi\", \"there\");      // 💥 error deep inside operator+\n}\n```\n\nThe compiler error for `\"hi\" + \"there\"` is huge and hard to read, because templates accept anything, and failure happens only inside the body.\n\n---\n\n# **2️⃣ What Are Concepts?**\n\n* **Concepts** = compile-time predicates (boolean conditions) on template parameters.\n* They check whether a type satisfies some properties (e.g., \"can be added\", \"is integral\").\n* If the constraint fails, the compiler gives a **clear error message**.\n\n---\n\n# **3️⃣ Example with `std::integral`**\n\n```cpp\n#include <concepts>\n#include <iostream>\n\ntemplate<std::integral T>\nT add(T a, T b) {\n    return a + b;\n}\n\nint main() {\n    std::cout << add(1, 2) << \"\\n\";   // ✅ OK\n    // std::cout << add(1.5, 2.3);   // ❌ Error: 'double' does not satisfy 'integral'\n}\n```\n\n✅ Now errors are **clean and descriptive**.\n\n---\n\n# **4️⃣ Common Standard Concepts**\n\nDefined in `<concepts>`:\n\n| Concept                      | Meaning                           |\n| ---------------------------- | --------------------------------- |\n| `std::integral<T>`           | T is an integer type              |\n| `std::floating_point<T>`     | T is float/double/long double     |\n| `std::signed_integral<T>`    | T is signed integer               |\n| `std::unsigned_integral<T>`  | T is unsigned integer             |\n| `std::same_as<T, U>`         | T and U are exactly the same type |\n| `std::derived_from<T, U>`    | T is derived from U               |\n| `std::convertible_to<T, U>`  | T can be converted to U           |\n| `std::invocable<F, Args...>` | F can be called with Args...      |\n\n---\n\n# **5️⃣ Custom Concepts**\n\nYou can define your own with `requires`.\n\n```cpp\n#include <concepts>\n#include <iostream>\n\ntemplate<typename T>\nconcept Addable = requires(T a, T b) {\n    { a + b } -> std::convertible_to<T>;\n};\n\ntemplate<Addable T>\nT add(T a, T b) {\n    return a + b;\n}\n\nint main() {\n    std::cout << add(1, 2) << \"\\n\";       // ✅ OK\n    // std::cout << add(\"hi\", \"there\");   // ❌ Error: 'const char*' doesn't satisfy Addable\n}\n```\n\n---\n\n# **6️⃣ Different Ways to Use Concepts**\n\n### (a) In template parameter list\n\n```cpp\ntemplate<std::integral T>\nT square(T x) { return x * x; }\n```\n\n### (b) As `requires` clause\n\n```cpp\ntemplate<typename T>\nrequires std::integral<T>\nT square(T x) { return x * x; }\n```\n\n### (c) As trailing `requires`\n\n```cpp\ntemplate<typename T>\nT square(T x) requires std::integral<T> {\n    return x * x;\n}\n```\n\n### (d) Abbreviated function template\n\n```cpp\nauto square(std::integral auto x) {\n    return x * x;\n}\n```\n\n---\n\n# **7️⃣ Why Are Concepts Useful?**\n\n* **Readable constraints** instead of SFINAE tricks (`std::enable_if`).\n* **Better compiler errors** (clear messages).\n* **Safer generic code** (functions won’t compile for nonsense types).\n* **Easier overloads** (select overloads based on concepts).\n\n---\n\n✅ **Summary:**\n\n* **Concepts** are compile-time checks on template parameters.\n* They replace ugly `enable_if` and SFINAE with clean, readable syntax.\n* C++20 provides standard concepts (`integral`, `floating_point`, etc.), and you can define your own.\n* They greatly improve error messages and generic programming.\n\n---\n\n👉 Would you like me to also show a **before vs after** example where the same template uses `enable_if` (pre-C++20) and concepts (C++20)? It really highlights why concepts are such a game-changer.\n",
  "Code/CMakeCourse/Lesson17Export/source/CMakeLists.txt": "cmake_minimum_required(VERSION 3.24.3)\nproject(Tutorial VERSION 1.0)\n\n# We create an interface library, tutorial_compiler_flags\nadd_library(tutorial_compiler_flags INTERFACE)\n\n# Add compiler feature cxx_std_14 to the target tutorial_compiler_flags\ntarget_compile_features(tutorial_compiler_flags INTERFACE cxx_std_14)\n\n# We determine which compiler our system is currently using to build, with a generator expression, \nset(gcc_like_cxx \"$<COMPILE_LANG_AND_ID:CXX,ARMClang,AppleClang,Clang,GNU,LCC>\")\nset(msvc_cxx \"$<COMPILE_LANG_AND_ID:CXX,MSVC>\")\n\n# We add the desired compiler warning flags that we want for our project. \ntarget_compile_options(tutorial_compiler_flags INTERFACE\n  \"$<${gcc_like_cxx}:$<BUILD_INTERFACE:-Wall;-Wextra;-Wshadow;-Wformat=2;-Wunused>>\"\n  \"$<${msvc_cxx}:$<BUILD_INTERFACE:-W3>>\"\n)\n\n# control where the static and shared libraries are built so that on windows\n# we don't need to tinker with the path to run the executable\nset(CMAKE_ARCHIVE_OUTPUT_DIRECTORY \"${PROJECT_BINARY_DIR}\")\nset(CMAKE_LIBRARY_OUTPUT_DIRECTORY \"${PROJECT_BINARY_DIR}\")\nset(CMAKE_RUNTIME_OUTPUT_DIRECTORY \"${PROJECT_BINARY_DIR}\")\n\noption(BUILD_SHARED_LIBS \"Build using shared libraries\" OFF)\n\n# Propagate a header file with the option\nconfigure_file(TutorialConfig.h.in TutorialConfig.h)\n\nadd_subdirectory(lib/math)\n\n#create Tutorial executable target\nadd_executable(Tutorial main.cpp)\n\n# We link our executable target and our math lib to our lib tutorial_compiler_flags\ntarget_link_libraries(Tutorial PUBLIC MathFunctions tutorial_compiler_flags)\n\n#\ntarget_include_directories(Tutorial PUBLIC\n                            \"${PROJECT_BINARY_DIR}\"\n                          )\n#\ninstall(TARGETS Tutorial DESTINATION bin)\ninstall(FILES \"${PROJECT_BINARY_DIR}/TutorialConfig.h\" DESTINATION include)\n\n\n\n# Tests\n# 1 - Add this line : \ninclude(CTest)\n\nenable_testing()\n\n# Testing if the application run\n# After NAME is the name of the test. After COMMAND is the executable to run.\n# Arguments of the executable can be added after the name of the executable if there are any.\nadd_test(NAME Runs COMMAND Tutorial 50)\n\n# does the usage message work?\n# Without any argument the executable is supposed to fail and return \"Usage: ./Tutorial number\"\n# We're testing if this message is printed like it should\nadd_test(NAME Usage COMMAND Tutorial)\nset_tests_properties(Usage\n  PROPERTIES PASS_REGULAR_EXPRESSION \"Usage:.*number\"\n  )\n\n# define a function to simplify adding tests\nfunction(do_test target arg result)\n  add_test(NAME Comp${arg} COMMAND ${target} ${arg})\n  set_tests_properties(Comp${arg}\n    PROPERTIES PASS_REGULAR_EXPRESSION ${result}\n    )\nendfunction(do_test)\n\n# do a bunch of result based tests\n# These tests will check if the string put as a regular expression is present in the output of the test\ndo_test(Tutorial 4 \"4 is 2\")\ndo_test(Tutorial 9 \"9 is 3\")\ndo_test(Tutorial 5 \"5 is 2.236\")\ndo_test(Tutorial 7 \"7 is 2.645\")\ndo_test(Tutorial 25 \"25 is 5\")\ndo_test(Tutorial -25 \"-25 is [-nan|nan|0]\")\ndo_test(Tutorial 0.0001 \"0.0001 is 0.01\")\n\n# Packaging\ninclude(InstallRequiredSystemLibraries)\nset(CPACK_RESOURCE_FILE_LICENSE \"${CMAKE_CURRENT_SOURCE_DIR}/License.txt\")\nset(CPACK_PACKAGE_VERSION_MAJOR \"${Tutorial_VERSION_MAJOR}\")\nset(CPACK_PACKAGE_VERSION_MINOR \"${Tutorial_VERSION_MINOR}\")\nset(CPACK_SOURCE_GENERATOR \"TGZ\")\ninclude(CPack)\n\n",
  "Code/CUDA-Linux/9_OptimisationPerformance.md/3_MaximiserLeDebitDinstructions.md": "# Maximiser le Débit d’Instructions\n\nPour **maximiser le débit d’instructions**, une application doit :\n\n- **Minimiser l’utilisation d’instructions arithmétiques à faible débit** ; cela inclut le compromis **précision vs. vitesse** quand il n’affecte pas le résultat final. Par exemple :  \n  - utiliser des **fonctions intrinsèques** au lieu de fonctions classiques (voir la section *Fonctions intrinsèques*),  \n  - préférer la **précision simple** (float) à la précision double (double),  \n  - ou **forcer la mise à zéro des nombres dénormalisés**.\n  - voir https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html?highlight=cudaMallocPitch#arithmetic-instructions\n\n- **Réduire les warps divergents** causés par les instructions de contrôle de flux (comme les branches `if/else`), tel que détaillé dans la section *Instructions de contrôle de flux*.\n\n- **Réduire le nombre d’instructions** :\n  - par exemple, en **optimisant ou supprimant les points de synchronisation** quand c’est possible. \n  - ou en utilisant des **pointeurs restreints** avec le mot-clé `__restrict__` (voir https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html?highlight=cudaMallocPitch#restrict)\n\n---\n\n### Debit d'instructions (instruction throughput)\n\nDans cette section, les débits sont exprimés en **nombre d’opérations par cycle d’horloge et par multiprocesseur**.  \nPour un **warp de 32 threads**, une **instruction = 32 opérations**.  \nDonc, si `N` est le nombre d’opérations par cycle, le **débit d’instructions** est `N / 32` instructions par cycle.\n\n**Remarque** : Tous les débits sont **par multiprocesseur**.  \nIl faut les **multiplier par le nombre de multiprocesseurs** présents sur le GPU pour obtenir le débit global de l’appareil.\n\n---\n\n### Fonctions Intrinsèques\n\nParmi ces fonctions, on trouve des versions **moins précises mais plus rapides** de certaines fonctions des **fonctions standard**.  \nElles portent le **même nom**, mais sont **préfixées par `__`**, comme par exemple : `__sinf(x)`.\n\nExemple :  \n- `sinf(x)` est la version standard (plus précise, mais plus lente)  \n- `__sinf(x)` est la version intrinsèque (moins précise, mais plus rapide)\n\nCes fonctions intrinsèques sont utiles quand la **performance est prioritaire** à la **précision maximale**.\n\n---\n\n### Instructions de contrôle de flux\n\nPour obtenir les meilleures performances dans les cas où le flux de contrôle dépend de l'identifiant du thread, la condition de contrôle doit être écrite de manière à minimiser le nombre de warps divergents. Cela est possible car la distribution des warps dans le bloc est déterministe, comme mentionné dans l’architecture SIMT. Un exemple trivial est lorsque la condition de contrôle dépend uniquement de `(threadIdx / warpSize)`, où `warpSize` est la taille du warp. Dans ce cas, aucun warp ne diverge, car la condition de contrôle est parfaitement alignée avec les warps. \n\n",
  "Code/DesignPatterns/Behavioral Paterns/9_StrategyPattern.md": "# Strategy Pattern\n\nThe Strategy pattern is a behavioral design pattern that enables you to define a family of algorithms, encapsulate each one of them, and make them interchangeable.\n\nIf we have for example a navigation app, we may want to provide different services, including routes to go from point A to point B.\nBut then we may want to add a service that shows walking routes.\nThen for cyclists.\nThen with public transports.\n\nAnd so on.\n\nVery fast the main class of our navigation class will increase in size and will be difficult to understand and maintain.\n\n## Solution with Strategy Pattern\n\nThe Strategy pattern that you take a class that does something specific in a lot of different ways and extract all of these algorithms into separate classes called strategies.\n\nThe original class is called the context.\nThe context holds a reference to one of the strategies and delegates the work to it.\n\nThe client passes the desired strategy to the context.\n\nThe context doesn't directly know about the strategies, it just works with them through an interface.\n\nThis way the context becomes independent of concrete strategies, so you can add new algorithms or modify existing ones without changing the code of the context or other strategies.\n\n## Structure\n\n1. **Context**: This is the class that uses the strategy objects. It contains a reference to a strategy interface and delegates the work to the strategy.\n\n2. **Strategy Interface**: This is an interface or abstract class that defines the contract for all strategies. It typically includes methods representing the algorithm or behavior.\n\n3. **Concrete Strategies**: These are concrete implementations of the strategy interface. Each concrete strategy provides a different way to perform a task or solve a problem.\n\n## Example with a travel planner\n\n```C++\n#include <iostream>\n#include <memory>\n#include <string>\n\n// Strategy interface\nclass TravelStrategy {\npublic:\n    virtual ~TravelStrategy() {}\n    virtual double calculateTime(int distance) const = 0;\n    virtual double calculateCost(int distance) const = 0;\n};\n\n// Concrete Strategies\n\nclass CarStrategy : public TravelStrategy {\npublic:\n    double calculateTime(int distance) const override {\n        // Assume an average speed of 60 km/h for a car\n        return static_cast<double>(distance) / 60.0;\n    }\n\n    double calculateCost(int distance) const override {\n        // Assume a cost of $0.50 per kilometer for a car\n        return 0.50 * distance;\n    }\n};\n\nclass BicycleStrategy : public TravelStrategy {\npublic:\n    double calculateTime(int distance) const override {\n        // Assume an average speed of 15 km/h for a bicycle\n        return static_cast<double>(distance) / 15.0;\n    }\n\n    double calculateCost(int distance) const override {\n        // Bicycles have no operating cost\n        return 0.0;\n    }\n};\n\nclass TrainStrategy : public TravelStrategy {\npublic:\n    double calculateTime(int distance) const override {\n        // Assume an average speed of 90 km/h for a train, including stops\n        return static_cast<double>(distance) / 90.0;\n    }\n\n    double calculateCost(int distance) const override {\n        // Assume a fixed cost of $0.05 per kilometer for a train\n        return 0.05 * distance;\n    }\n};\n\n// Context class\nclass TravelPlanner {\nprivate:\n    std::unique_ptr<TravelStrategy> strategy;\n\npublic:\n    TravelPlanner(std::unique_ptr<TravelStrategy> strategy) : strategy(std::move(strategy)) {}\n\n    void setStrategy(std::unique_ptr<TravelStrategy> newStrategy) {\n        strategy = std::move(newStrategy);\n    }\n\n    void planTravel(const std::string& mode, int distance) const {\n        double time = strategy->calculateTime(distance);\n        double cost = strategy->calculateCost(distance);\n        std::cout << \"Traveling \" << distance << \" km by \" << mode << \":\\n\";\n        std::cout << \"Time: \" << time << \" hours\\n\";\n        std::cout << \"Cost: $\" << cost << std::endl;\n    }\n};\n\nint main() {\n    TravelPlanner planner{std::make_unique<CarStrategy>()};\n    planner.planTravel(\"car\", 100);\n\n    planner.setStrategy(std::make_unique<BicycleStrategy>());\n    planner.planTravel(\"bicycle\", 100);\n\n    planner.setStrategy(std::make_unique<TrainStrategy>());\n    planner.planTravel(\"train\", 100);\n\n    return 0;\n}\n\n```\n\n## Use cases\n\nThe Strategy pattern is a versatile design pattern that finds applications in various domains. Here are some common use cases where the Strategy pattern is beneficial:\n\n1. **Sorting Algorithms**: In software that requires sorting functionality, such as database systems or search algorithms, the Strategy pattern can be used to encapsulate different sorting algorithms (e.g., bubble sort, merge sort, quick sort). This allows the algorithm to be selected or switched dynamically based on factors like data size or type.\n\n2. **Payment Processing**: In e-commerce applications, the Strategy pattern can be employed to handle different payment methods (e.g., credit card, PayPal, bank transfer). Each payment method can be encapsulated as a strategy, enabling the system to support multiple payment options while keeping the payment processing logic decoupled from the core application.\n\n3. **Compression Algorithms**: Applications that involve data compression or decompression, such as file compression utilities or network protocols, can benefit from the Strategy pattern. Different compression algorithms (e.g., gzip, zip, LZ4) can be implemented as strategies, allowing the application to switch between them based on performance requirements or compatibility constraints.\n\n4. **Authentication and Authorization**: Systems that require authentication and authorization mechanisms can use the Strategy pattern to handle different authentication methods (e.g., username/password, OAuth, biometric authentication). Each authentication method can be implemented as a strategy, enabling the system to accommodate various security requirements or user preferences.\n\n5. **Game AI Behavior**: In game development, the Strategy pattern can be utilized to define different behaviors for non-player characters (NPCs) or game entities. Each behavior (e.g., aggressive, defensive, passive) can be implemented as a strategy, allowing NPCs to adapt their actions dynamically based on game events, player interactions, or environmental conditions.\n\n6. **Text Processing and Parsing**: Applications that involve text processing, parsing, or lexical analysis can employ the Strategy pattern to handle different parsing or tokenization strategies. For example, a parser for a programming language may use different strategies for parsing expressions, statements, or declarations, allowing for modular and extensible code.\n\n7. **User Interface Components**: Graphical user interface (GUI) frameworks often use the Strategy pattern to define different behaviors for UI components (e.g., buttons, menus, dialogs). Each behavior (e.g., onClick behavior, onMouseOver behavior) can be implemented as a strategy, enabling customization and reuse of UI components across different applications.\n\nOverall, the Strategy pattern provides a flexible and maintainable way to encapsulate algorithms or behaviors, allowing them to vary independently from the clients that use them. This promotes code reuse, modularity, and extensibility, making it easier to adapt software systems to changing requirements or use cases.\n\n## Alternative\n A lot of modern programming languages have functional type support that lets you implement different versions of an algorithm inside a set of anonymous functions. Then you could use these functions exactly as you’d have used the strategy objects, but without bloating your code with extra classes and interfaces.",
  "Code/FormationTableauRPython/Python/Fondamentaux/Lesson7Modules/packaging_tutorial/README.md": "# Example Package\n\nThis is a simple example package. You can use\n[Github-flavored Markdown](https://guides.github.com/features/mastering-markdown/)\nto write your content.",
  "Code/MatlabDeepLearning/0_Exercices/5_Ex_CNN/1_Ex_CNN.md": "# 1 -  CNN\n- Réécrire l'exemple\n- Observer les données, visualiser les images\n- Comprendre les dimensions des entrées/sorties des différentes couches\n- Comprendre l'effet du padding et stride sur les dimensions\n- Comprendre l'effet de la couche MaxPooling sur la dimension des données\n- Observer le sortie de chaque couche en mode inférence. Utiliser la fonction forward pour cela.\n\n# 2 - Datastores\n\n- Réécrire l'exemple\n- Regarder comment fonctionne imageDataStore, les différents champs\n- Observer les images à partir de imageDataStore\n- Récupérer toutes les données de l'augmentedDataStore avec la fonction read.\n- Visualiser les images augmentées\n- Essayer d'autres types d'augmentation des images\n- Forcer l'execution sur le CPU (dans les training options), et voir la différence de performance avec le GPU\n- Tester différentes tailles de batch (en commençant petit) et visualiser l'empreinte mémoire (avec top, ...). \n- Regarder l'usage CPU. Vérifier combien de threads CPU Matlab peut exécuter, et si en dessous du max, mettre au max\n- Evaluer le modèle sans augmentation, puis avec augmentation, puis en ajoutant une régularisation dropout\n\n# 3 - Modèle préentrainé\n\n- Réécrire l'exemple\n- Importer le modèle à partir du fichier ONNX\n- Observer le modèle à partir de l'objet dlnetwork\n- Observer le modèle avec l'outil deepNetworkDesigner\n- Comprendre ses entrées/sorties\n- Créer un nouveau classifieur\n- Faire passer les données dans le backbone importé et récupérer les cartes de caractéristiques\n- Comprendre les données de sortie du backbone\n- Classifier les données grâce au nouveau classifieur\n- Plot des fonctions de loss et de l'accuracy pour l'entrainement et la validation\n- Chercher à évaluer s'il y a surapprentissage\n- Evaluer le modèle sur les données de test\n  \n- Trouver un autre jeu de données et tester à nouveau\nhttps://www.mathworks.com/help/deeplearning/ug/data-sets-for-deep-learning.html\n\n# 4 - Modèle intégré\n\n- Créer le modèle intégré\n- Comprendre ce qu'est le gel de paramètres, quels paramètres geler et comment\n- geler les couches du backbone\n- Observer, évaluer, tester\n\n# 5 - Fine tuning\n\n- Faire l'ajustement fin du modèle sur les données chat/chien ou un autre jeu d'image\n- Voir ce que cela donne si on utilise un autre redimensionnement pour les images\n- Observer/évaluer/tester \n\n# 6 - Architecture\n\n- Réécrire un bloc de base de l'architecture Xception41, avec separable convolution, batch normalisation et connection résiduelle",
  "Code/MatlabSimulinkComm/0_Exercices/12_Ex_OFDM/1_Ex_OFDM.md": "# Exercices OFDM\n\n## 1. Construire un système OFDM suréchantillonné en canal SISO sous Simulink\n\n### Objectif\n\nConstruire un modèle Simulink qui simule un système OFDM 16-QAM suréchantillonné, transmettant 3 symboles à travers un canal SISO bruité, avec insertion de pilotes, préfixe cyclique variable, et estimation de puissance du signal bruité.\n\n---\n\n### Paramètres donnés\n\n```matlab\nM = 16;                 % Ordre de modulation (16-QAM)\nnfft = 64;              % Longueur FFT (nombre total de sous-porteuses)\nNumSymbols = 3;         % Nombre de symboles OFDM\nosf = 4;                % Facteur de suréchantillonnage\nngbc = [9; 8];          % Porteuses de garde gauche/droite\ninsertdcnull = true;    % Insertion de la porteuse DC\naddpilotport = true;    % Activation du port de pilotes\npscindx = [[12;26;40;54],[14;28;38;52],[12;26;40;54]];  % Indices des pilotes\ncplen = [16 32 16];     % Longueur du préfixe cyclique pour chaque symbole\n```\n\n---\n\n### 1. Configuer les paramètres de bloc\nDans la fonction PreLoadFcn, rentrer les valeurs de paramètres donnés.\n\n### 2. Calcul du nombre d’échantillons par trame\n\n#### a) **Nombre total de sous-porteuses actives**\n\n* Sous-porteuses actives = FFT length – nombre total de porteuses de garde\n* On ajoute 1 si `insertdcnull = true`\n\n```matlab\nnasc = nfft - sum(ngbc) + insertdcnull;\n```\n\n---\n\n#### b) **Nombre d’échantillons de données (hors pilotes)**\n\nOn veut générer 3 symboles OFDM au total. Ces symboles OFDM sont constitués par les symboles QAM, transmis en parralèle sur les différentes sous porteuses qui portent des données (et non pas des pilotes ou le DC null, ou les bandes de guarde).\nCalculer le nombre d'échantillions de données (QAM) nécéssaires à générer.\nCela nécessitera de calculer combien de sous porteuses peuvent contenir des données.\n\nDans la variable spf_data.\n\n---\n\n#### ✅ c) **Nombre d’échantillons de pilotes**\n\nCalculer le nombre d'échantillons QAM nécéssaires pour les pilotes (toujours avec 3 symboles OFDM en vue).\n\nDans la variable spf_pilot.\n\n### Étape 2 : Calcul de la puissance attendue du signal bruité\n\nCalculer la puissance du signal OFDM, pour pouvoir la renseigner dans le bloc AWGN Channel.\nVariable awgnInSigPwr\n\n---\n\n### Tâches Simulink à réaliser\n\n1. **Générez** des données aléatoires (data et pilotes) avec deux blocs **Random Integer Generator** (symboles, pas en bits)\n2. **Modulez** les deux flux avec deux blocs **16-QAM Modulator Baseband**.\n3. **Appliquez un Reshape** sur chaque flux pour obtenir des matrices de la bonne taille pour chaque flux en entrée du bloc OFDM.\n4. **Configurez le bloc OFDM Modulator Baseband** avec les valeurs données et calculées\n5. **Transmettez** le signal dans un **bloc AWGN Channel** :\n\n   * Configurez la puissance d’entrée avec `awgnInSigPwr`\n   * Choisissez un SNR (ex. 14 dB)\n6. **Affichez** le signal modulé et bruité avec des scopes (Time ou Spectrum).\n7. Ajoutez un **bloc OFDM Demodulator**\n8. Ajouter deux blocs de démodulation QAM, pour les signaux de données et pour les pilotes.\nInsérer des blocs Reshape avant les blocs QAM pour avoir le bon format matriciel.\n9. Calculer le SER pour chaque flux\n10. Vérifier, en normalisant le signal OFDM avec un bloc Gain que la puissance est bien normalisée (ou que le signal RMS est à 1, après un bloc RMS). Cela permet de vérifier que les valeurs entrées dans le bloc ont bien les effets escomptés.\n\n---\n\n# 2. MIMO OFDM Equalizer\n\nFaire énoncé pour https://www.mathworks.com/help/comm/ref/ofdmequalizerblock.html. Voir cours OFDM Equalization.\n\nApply equalization to an OFDM-modulated QAM signal that has been filtered through a Rayleigh MIMO channel.\n\n# 3. OFDM Equalizer\n\nVoir https://www.mathworks.com/help/wireless-hdl/ug/equalizeofdmdatausingchannelestimates.html\n\n",
  "Code/Python/Lesson7Modules/packaging_tutorial/README.md": "# Example Package\n\nThis is a simple example package. You can use\n[Github-flavored Markdown](https://guides.github.com/features/mastering-markdown/)\nto write your content."
};
